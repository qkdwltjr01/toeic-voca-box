# 원본 PDF (15MB) — 단어 데이터는 words.json 에 이미 추출되어 있다.
# 다시 파싱하려면 이 파일을 이 폴더에 두고 `python build.py` 실행.
토익-단어장-PDF-for-PC-A4size-All.pdf
*.pdf

# 앱에서 내보낸 개인 학습 기록
단어상자-백업-*.json

# OS
Thumbs.db
desktop.ini
.DS_Store

# 강의 자료 (남의 강의 전사본 — 이 저장소는 public이라 올리지 않는다).
# 바탕화면 폴더에는 그대로 두고 로컬에서만 본다.
서아쌤_*
